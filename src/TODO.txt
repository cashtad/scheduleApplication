TODO list:
 2) "SessionStatusSyncService не сохраняет сессию": Статусы обновляются в памяти, но не автосохраняются после ingestion.
 Если хочешь “после релода всё восстановилось ровно как было”, нужно либо:
  - вызывать SaveSessionUseCase после sync
  - либо делать orchestration-уровнем сохранение.
 3) AnalyzeWorkflowService всегда строит repository. Это ок, но если ingestion сильно сломан, можно early-block до build (опционально). Сейчас не критично.
 4) мб использовать tuple вместо frozenset?
 5) решить вопрос с обработкой ошибок при парсинге, при составлении репозитория
  и что и в каких случаях отображать пользователю
 6) сделать так, чтобы при повторном открытии листа маппинг сразу вставал на свои места, а не требовал повторного выбора
 7) Импорты/запуск PyCharm + .exe (single-file и one-folder)
 8) Restore status после перезапуска. Таблицы должны быть маппед, потом проверены и только потом ready
 9) readiness policy
 10) Просмотр последнего отчета (в app и браузере)
 11) Просмотр нарушений в таблице расписания
 12) Просмотр ошибок/предупреждений ingestion/validation